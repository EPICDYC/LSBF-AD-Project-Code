import tkinter as tk  # you can give a "short name" for the import file - Window is a variable name

window = tk.Tk()
window.title('This is the window for the GUI')

# start the window loop
window.mainloop()
